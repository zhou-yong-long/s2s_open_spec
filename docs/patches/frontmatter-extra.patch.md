# Frontmatter extra keys (implemented)

Unknown YAML keys in spec frontmatter are preserved when the CLI updates frontmatter.

- Implementation: [`src/core/spec.ts`](../../src/core/spec.ts) (`frontmatterExtra`, `normalizeFrontmatter`, `updateFrontmatter`).
- Tests: [`tests/core/spec.test.ts`](../../tests/core/spec.test.ts).

This file remains as a pointer for anyone who read earlier drafts of the delivery plan.
